name: chatgpt_auth_kit
description: Sign in with a ChatGPT subscription and call OpenAI Codex's Responses API. Pure-Dart OAuth + streaming client.
version: 0.1.0
homepage: https://github.com/mobai-app/chatgpt-auth-kit-flutter
repository: https://github.com/mobai-app/chatgpt-auth-kit-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  url_launcher: ^6.2.0
  crypto: ^3.0.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0

flutter:
